= render :partial => 'admin/shared/nav'
%h1 Events
%table{:class => "index"}
  %tr
    %th{:width => "250px"}
      Title
    %th{:width => "200px"}
      Calendar
    %th
  - for event in @events
    %tr.node.level-0{:class => event.ical_linked? ? "ical_event" : ""}
      %td{:class => "page", :style => "padding-left: 20px"}
        - link_to_unless_block(event.ical_linked?, edit_admin_event_path(event)) do
          %span.w1
            %img{:src => "/images/admin/page.png", :class => "icon", :alt => "page-icon"}
            %span.title
              = event.title
              = "(iCal)" if event.ical_linked?
      %td{:width => "200px"}
        = event.calendar.name
      %td{:style => "text-align:right;"}
        = link_to(image_tag("/images/admin/remove.png", :alt => "remove event"), admin_event_path(event), :confirm => 'Are you sure?', :method => :delete) unless event.ical_linked?
%br
%div{:style => "text-align:right;width:100%;"}
  = link_to 'Add an event', new_admin_event_url
%p * These are manually entered and managed events, any iCal Subscription generated events are hidden.
  