= render :partial => 'admin/shared/nav'
%h3 
  Editing Calendar:
  = "#{@calendar.name} (calendars/#{@calendar.category.downcase}/#{@calendar.name.downcase})"
= render :partial => "form", :locals => { :calendar => @calendar }